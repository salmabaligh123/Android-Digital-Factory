fun main(){
     val collection : List<String> = listOf("Maze","Mr.","More")
     
     for(item in collection){
         if(item.startsWith("M") && (item.contains("r"))){
             println(item)
             break
         }
         else{
             println("default result")
         }
     }
}
