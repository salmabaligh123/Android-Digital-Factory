fun main(){
     val collection : List<String> = listOf("Maze","Mr.","More")
     
     for(item in collection){
         if(item.startsWith("M") && ("r" in item)){
             println(item)
             break
         }
     }
}
