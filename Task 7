fun main(){
     val collection : List<String> = listOf("a","Ma","M")
     
     for(item in collection){
         if(item.startsWith("M") && (item.contains("a"))){
             val index = collection.indexOf(item)-1
             val ch = collection.get(index)
             println(ch)
             break
         }
     }
}
